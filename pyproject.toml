[project]
name = "cowork-orchestrator"
version = "0.1.0"
description = "Scaffold for a GitHub-gated AI review orchestrator"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
